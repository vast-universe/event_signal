# 数据库配置 (必填)
# Vercel Postgres / Neon
# 注意: 使用 postgresql+asyncpg:// 前缀，ssl=require (不是 sslmode)
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/database?ssl=require

# API配置 (可选)
API_HOST=0.0.0.0
API_PORT=8000
